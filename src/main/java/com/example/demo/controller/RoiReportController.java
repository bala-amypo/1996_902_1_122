package com.example.demo.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class RoiReportController {
    // Intentionally empty — controller existence is tested via DI only
}
